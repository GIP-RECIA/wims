!set titb=Algorithme pour une autre fonction
!set keyw=
!set datm=20210913
!set prev=embedExpQAlg3
!set next=embedExpQAlg5
!set upbl=main
!set dat1=19000101
!set dat2=24000101

!if $wims_read_parm!=$empty
  !goto $wims_read_parm
!endif
!exit

:content
<p>
L'algorithme présenté ci-dessus est prévu pour la fonction cube qui est une fonction strictement croissante.<br>
Pour une autre fonction, il faut changer l'instruction 
!insmath Y \leftarrow X^3
 pour y mettre la formule de la fonction,
mais cet algorithme ne fonctionnera pas correctement si la fonction n'est pas croissante.</p>

